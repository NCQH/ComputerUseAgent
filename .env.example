# Copy to .env and fill in the key for the provider you use.
# .env is gitignored — never commit your real keys.

# Anthropic (Claude) — used with: python -m cua --provider claude
ANTHROPIC_API_KEY=

# OpenAI — used with: python -m cua --provider openai
OPENAI_API_KEY=
